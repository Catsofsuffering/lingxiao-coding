[package]
name = "lingxiao-tool-host-protocol"
version = "0.1.0"
description = "External tool-host protocol — sidecar envelope, tool call/response, cancellation, resource budget"
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
